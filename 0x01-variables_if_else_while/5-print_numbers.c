#include <stdio.h>

/**
 * main - entry point
 *
 * Description: print digits number from base 10.
 *
 * Return: 0 (success)
*/
int main(void)
{
	int digit;

	for (digit = 0; digit < 10; digit++)
{
		printf("%d", digit);
}
	printf("\n");
	return (0);
}
